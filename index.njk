---
layout: "baseof.njk"
title: "Vegan Homecooking by Tiff & Laura"
---

<div class="top-container md:mx-auto">
  <div
    class="max-w-screen-sm mb-12 mx-auto md:p-12 p-8 bg-gray-700 text-gray-100"
  >
    <h1 class="text-2xl mb-2 font-display">
      {{ title }}
    </h1>
    <p class="mb-4">
      A personal reference and chronicle for our favorite vegan original and
      adapted vegan recipes. Free of ads or long SEO preludes.
    </p>
  </div>
  <div class="p-8">
    <h2 id="recipes" class="text-4xl mb-6 font-display text-center">Recipes</h2>
    <ul class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
      {% for item in collections.recipe %}
      <li class="">{% include "recipeList.njk" %}</li>
      {% endfor %}
    </ul>
  </div>
  <!--<div class="md:w-1/3 mb-8">
    <div class="">
      <h2 class="top-header text-center">All Categories</h2>
      <ul class="flex flex-wrap justify-center items-center text-center text-lg">
        {% for tag in collections.tagList %}
        <li class="m-2 bg-red-700 text-gray-50 px-2 py-1">
          <a href="/tags/{{ tag }}" class="block py-1">{{ tag }}</a>
        </li>
        {% endfor %}
      </ul>
    </div>
  </div> -->
</div>
