---
layout: "baseof.njk"
title: "Vegan Homecooking by Tiff & Laura"
---

<div class="top-container">
  <div class="max-w-screen-sm mb-12 mx-auto p-4 md:p-8 bg-yellow-50">
    <h1 class="top-header">
      {{ title }}
    </h1>
    <p class="mb-4">
      A pretty website free of ads, long SEO preludes, or weird design choices
      to chronicle some of our favorite original and adapted recipes.
    </p>
  </div>
  <div class="md:w-2/3 md:p-4 mb-8 bg-yellow-50">
    <div class="p-4">
      <h2 class="top-header text-center">A Few of Our Favorites</h2>
      <ul class="flex flex-wrap">
        {% for item in collections.recipe %}
        <li class="md:w-1/2 md:p-2 h-full relative">
          {% include "recipeList.njk" %}
        </li>
        {% endfor %}
      </ul>
    </div>
  </div>
  <div class="md:w-1/3 md:p-4 mb-8">
    <div class="p-4">
      <h2 class="top-header text-center">All Categories</h2>
      <ul class="flex flex-wrap justify-center items-center text-center">
        {% for tag in collections.tagList %}
        <li class="m-2 bg-green-200">
          <a href="/tags/{{ tag }}" class="block py-1">{{ tag }}</a>
        </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</div>
