{
  "name": "recipes",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:styles": "postcss css/tailwind.css -o css/styles.css",
    "build:pages": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "dev": "npm-run-all build:styles serve",
    "build": "npm-run-all build:styles build:pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^0.12.1",
    "autoprefixer": "^10.1.0",
    "markdown-it-anchor": "^7.0.2",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.2.1",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.2.7"
  }
}
